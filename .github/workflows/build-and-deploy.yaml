name: Build and Deploy

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  IMAGE_REGISTRY: ghcr.io
  IMAGE_REPO: ghcr.io/0xpuncker/lab-bittensor
  BT_VERSION: "10.3.0"
  PYTHON_VERSION: "3.11"

jobs:
  sensors:
    name: Run Sensors
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Cache pip packages
        uses: actions/cache@v5
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install mypy ruff pytest pip-audit bandit[toml] detect-secrets
          pip install -r requirements.txt
          # Pre-install commonly used packages for faster subsequent runs
          pip install --upgrade bittensor kubernetes

      - name: Typecheck (mypy)
        run: mypy --config-file mypy.ini || true

      - name: Lint (ruff)
        run: ruff check .

      - name: Test (pytest)
        run: pytest --tb=short -q --ignore=tests/integration/ --ignore=tests/test_smoke.py || true

      - name: Audit (pip-audit)
        run: pip-audit --desc || true

      - name: Security (bandit)
        run: bandit -r . -c pyproject.toml -ll || true

      - name: Secret scan
        run: detect-secrets scan --baseline .secrets.baseline || true

  build:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    needs: sensors
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.IMAGE_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.IMAGE_REPO }}
          tags: |
            type=raw,value=${{ env.BT_VERSION }}-{{sha}}
            type=raw,value=${{ env.BT_VERSION }}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Extract image tag
        id: tag
        run: |
          # metadata-action outputs :latest first; grep for the versioned-sha tag explicitly
          IMAGE_TAG=$(printf '%s' "${{ steps.meta.outputs.tags }}" | grep -E ":${{ env.BT_VERSION }}-[a-f0-9]+" | head -n1)
          echo "tag=${IMAGE_TAG}" >> $GITHUB_OUTPUT
          echo "${IMAGE_TAG}" > /tmp/image-tag.txt

      - name: Upload image tag artifact
        uses: actions/upload-artifact@v7
        with:
          name: image-tag
          path: /tmp/image-tag.txt

  update_k8s_manifests:
    name: Update K8s Manifests
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: write
    steps:
      - name: Checkout lab-bittensor
        uses: actions/checkout@v5

      - name: Download image tag
        uses: actions/download-artifact@v8
        with:
          name: image-tag
          path: ./artifact

      - name: Set IMAGE_TAG env
        run: |
          # artifact contains the full image reference, e.g. ghcr.io/0xpuncker/lab-bittensor:10.3.0-abc1234
          IMAGE_TAG=$(cat artifact/image-tag.txt)
          echo "IMAGE_TAG=${IMAGE_TAG}" >> $GITHUB_ENV
          SHORT_SHA="${GITHUB_SHA:0:7}"
          echo "SHORT_SHA=${SHORT_SHA}" >> $GITHUB_ENV

      - name: Update K8s manifests
        run: |
          echo "Updating K8s manifests to use image: ${IMAGE_TAG}"
          # Use the full IMAGE_TAG (includes branch prefix, e.g. main-abc1234) to match what was pushed
          sed -i "s|image: ghcr.io/0xpuncker/lab-bittensor:.*|image: ${IMAGE_TAG}|g" k8s/bittensor-testnet.yaml
          git diff k8s/bittensor-testnet.yaml

      - name: Commit and push K8s manifest update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add k8s/bittensor-testnet.yaml
          git diff --staged --quiet || git commit -m "chore(k8s): update image tag to ${SHORT_SHA} [ci skip]"
          git pull --rebase origin main
          git push
